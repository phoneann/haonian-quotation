# 呱呱大腦 — 好年企業專案（Gemini 入口）

本專案唯一的完整記憶檔是 [`CLAUDE.md`](./CLAUDE.md)。開始工作前請先讀取該檔案，
裡面有主人資訊、專案狀況、待辦事項、以及「AI協作日誌」——這份日誌是 Claude / Codex /
Gemini 三個工具共用的記憶，不管主人用手機還是桌機、叫的是哪個AI，規則都一樣。

## 規則（跟 CLAUDE.md 裡「重要提醒」一致，Gemini 也要遵守）
- 開工前先讀 `CLAUDE.md` 全文，掌握最新狀況
- 每次工作session結束前，不用主人開口，主動在 `CLAUDE.md` 的「AI協作日誌」區塊
  新增一行：`- YYYY-MM-DD HH:MM [Gemini/裝置] 做了什麼`，然後 commit + push
- 就算這次只是聊天討論、沒有實際改程式碼，也要記一行，避免其他裝置/工具看不到
- 不要把內容複製到這份檔案裡維護——這裡只是入口，唯一真實內容都在 `CLAUDE.md`
